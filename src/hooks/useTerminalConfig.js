/**
 * Hook that loads and returns the config object for the currently selected terminal.
 * Abstracts JSON import so components are decoupled from config file paths.
 * TODO: Accept a terminalId param and dynamically return the matching config.
 */

export function useTerminalConfig(terminalId) {
  // TODO: return config object for the given terminalId
  return null;
}
