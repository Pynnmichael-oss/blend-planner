/**
 * JSON schema and type documentation for terminal config files (fort-worth.json, tampa.json, etc.).
 * Provides field definitions and validation helpers consumed by useTerminalConfig.
 * TODO: Add runtime validation (e.g. zod or manual checks) against this schema.
 */

export const TERMINAL_SCHEMA = null;
