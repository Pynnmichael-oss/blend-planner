/**
 * Generic status badge pill used across the UI to indicate
 * states such as "blend", "no-blend", "override", or "warning".
 * TODO: Accept status and label props; map status to colour variants.
 */

export default function StatusBadge() {
  return null;
}
